/* Selecionar os dados de todas as colunas da tabela de acordo com uma condição */

SELECT * FROM nome_tabela WHERE condição;

/* Selecionar os dados de uma ou mais colunas espefícicas de acordo com uma condição*/

SELECT nome_coluna, nome_coluna 
FROM nome_tabela WHERE condição;
