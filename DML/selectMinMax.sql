/* Seleciona os menores e maiores valores de uma coluna de uma tabela */

/* Menor valor */
SELECT MIN(nome_coluna) FROM nome_tabela WHERE condição = valor;

/* Maior valor */
SELECT MAX(nome_coluna) FROM nome_tabela WHERE condição = valor;