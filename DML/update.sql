UPDATE nome_tabela SET nome_coluna = valor WHERE condição = valor;

/* Alterando vários campos de uma única vez */

UPDATE nome_tabela SET 
nome_coluna_1 = valor, 
nome_coluna_2 = valor, 
nome_coluna_3 = valor 
WHERE condição = valor;