
/* É usado para selecionar valores que são iguais a um ou outro valor da condição*/
SELECT * FROM nome_tabela WHERE condição IN (valor1, valor2)

/* É usado para selecionar valores que são diferentes dos valores das condições */
SELECT * FROM nome_tabela WHERE condição NOT IN (valor1, valor2)
