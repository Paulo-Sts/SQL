/* É usado para selecionar valores em um determinado intervalo */

SELECT * FROM nome_tabela WHERE nome_coluna BETWEEN valor1 AND valor2; /* O Berween é inclusivo, então os valores das pontas caso existam também são incluídos no resultado */

